sqlite3 shop.sqlite3
CREATE TABLE CATEGORY (
  CATEGORY_ID INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  CATEGORY_NAME TEXT NOT NULL
);
INSERT INTO CATEGORY (CATEGORY_NAME) VALUES
  ('clothing'),
  ('shoes'),
  ('clocks'),
  ('furniture');
CREATE TABLE GOOD (
  GOOD_ID INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  CATEGORY_ID INTEGER NOT NULL,
  GOOD_NAME TEXT NOT NULL,
  PRICE NUMERIC NOT NULL,
  FOREIGN KEY (CATEGORY_ID) REFERENCES CATEGORY(CATEGORY_ID)
);
INSERT INTO GOOD (GOOD_NAME, PRICE, CATEGORY_ID) VALUES
  ('Fellini' , '1.00', '2'),
  ('Dino Richi', '1.00', '2'),
  ('Marc O`Polo', '1.00', '1'),
  ('Mexx', '1.00', '3'),
  ('Mexx', '1.00', '4'),
  ('Adidas', '1.00', '2'),
  ('Puma', '1.00', '1'),
  ('ECCO', '1.00', '2'),
  ('Colins', '1.00', '1'),
  ('Zenith', '1.00', '3'),
  ('Orient', '1.00', '3'),
  ('Patek Phillip', '1.00', '3'),
  ('Zarina', '1.00', '4');
UPDATE GOOD SET PRICE=3.50
 WHERE GOOD_ID=1;
UPDATE GOOD SET PRICE=PRICE*1.1;
DELETE FROM GOOD WHERE GOOD_ID=2;
SELECT * FROM GOOD ORDER BY GOOD_NAME ASC;
SELECT * FROM GOOD ORDER BY PRICE DESC;
SELECT GOOD_NAME, PRICE FROM GOOD ORDER BY PRICE DESC LIMIT 3;
SELECT GOOD_NAME, PRICE FROM GOOD ORDER BY PRICE LIMIT 3;
SELECT GOOD_NAME, PRICE FROM GOOD ORDER BY PRICE LIMIT 3, 3;
SELECT GOOD_NAME FROM GOOD WHERE PRICE IN
  (SELECT MAX(PRICE) FROM GOOD);
SELECT GOOD_NAME FROM GOOD WHERE PRICE IN
  (SELECT MIN(PRICE) FROM GOOD);
SELECT COUNT(*) FROM GOOD;
SELECT AVG(PRICE) FROM GOOD;